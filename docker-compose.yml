services:
  hugo:
    image: hugomods/hugo:debian-dart-sass-git-non-root-0.163.3
    volumes:
      - ./:/src
    ports:
      - "1313:1313"
    command: "hugo server --buildDrafts --noHTTPCache --bind=0.0.0.0"
